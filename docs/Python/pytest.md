## Pytest

pytest used for testing.  To run

```
pytest {folder_with_tests}
```

It will look for all files with **_test** in the name and try to run them, so point it at the folder where the tests are. 